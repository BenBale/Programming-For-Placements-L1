#include <iostream>
#include <stack>
#include <algorithm>
using namespace std;

int main() {
	// your code goes here
	stack <int> st;
	int n;
	cin>>n;
	for (int i=1;i<=n;i++)
	 st.push(i);
	 
	 cout<<st.top()<<endl;
	st.pop();
	cout<<st.size();
	
	
	return 0;
}
